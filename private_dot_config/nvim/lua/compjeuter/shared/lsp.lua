-- All lsp's that are installed and used on the configuration level. 
-- One might add their own manually.
return {
    gopls = {}, -- golang
    ts_ls = {}, -- typescript, javascript (with JSDoc)
    lua_ls = {}, -- lua
    intelephense = {}, -- php (sadly)
    html = {}, -- html
    cssls = {}, -- css
    svelte = {}, -- svelte
    bashls = {},
}
